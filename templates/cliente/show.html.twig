{% extends 'base.html.twig' %}

{% block title %}Cliente{% endblock %}

{% block body %}
    <h1>Cliente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cliente.id }}</td>
            </tr>
            <tr>
                <th>Rut</th>
                <td>{{ cliente.rut }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cliente.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido Paterno</th>
                <td>{{ cliente.aPaterno }}</td>
            </tr>
            <tr>
                <th>Apellido Materno</th>
                <td>{{ cliente.aMaterno }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cliente.email }}</td>
            </tr>
            <tr>
                <th>Teléfono</th>
                <td>{{ cliente.telefono }}</td>
            </tr>
        </tbody>
    </table>
    <div class="inline">
        <a class="btn btn-secondary" href="{{ path('cliente_index') }}">Volver al listado</a>

        <a class="btn btn-primary" href="{{ path('cliente_edit', {'id': cliente.id}) }}">Editar</a>        
                
        {{ include('cliente/_delete_form.html.twig') }}                
    <div>
{% endblock %}
